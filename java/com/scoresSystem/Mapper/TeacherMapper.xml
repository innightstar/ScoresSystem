<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scoresSystem.Mapper.TeacherMapper">
    <select id="findAll" resultType="com.scoresSystem.pojo.Teacher">
        SELECT * FROM tb_teacher
    </select>
    <select id="findByTeacherName" resultType="com.scoresSystem.pojo.Teacher">
        SELECT * FROM tb_teacher WHERE name LIKE CONCAT('%', #{teacherName}, '%');
    </select>
    <select id="getTeacherById" resultType="com.scoresSystem.pojo.Teacher">
        SELECT * FROM tb_teacher where id=#{id}
    </select>
    <select id="loginTeacher" resultType="com.scoresSystem.pojo.Teacher">
        SELECT * FROM tb_teacher WHERE name=#{param1} AND password=#{param2}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyColumn="id">
        INSERT INTO tb_teacher VALUES(null,#{name},#{password},#{age},#{sex},#{valid})
    </insert>
    <update id="update">
        UPDATE tb_teacher SET valid=0 WHERE id=2
    </update>
    <delete id="delete">
        DELETE FROM tb_teacher WHERE id=#{id}
    </delete>
</mapper>